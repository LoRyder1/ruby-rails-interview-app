<li>
  <div>
    <%= material.name %> | Amount: <%= material.amount %> | 
    <%= link_to 'Edit', edit_material_path(material) %> |
    <%= link_to 'Destroy', material_path(material), method: :delete, data: {confirm: "Are you sure?"} %>
  </div>
</li>
